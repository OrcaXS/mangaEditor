# mangaEditor


## Build Setup

``` bash
# add API config
$ cp config.example.js config.js
$ $EDITOR config.js

# install dependencies
$ yarn install

# serve with hot reload (js only) at localhost:9000
$ yarn dev

# build for production and launch server
$ yarn build
$ yarn start

